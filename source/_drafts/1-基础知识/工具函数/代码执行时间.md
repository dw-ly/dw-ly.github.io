```c++

#include<iostream>
#include <chrono>
 
using namespace std;
 
int main()
{
 
chrono::steady_clock::time_point t1 = chrono::steady_clock::now();
 
//需测试运行时间的代码
 
chrono::steady_clock::time_point t2 = chrono::steady_clock::now();
 
chrono::duration<double> time_used = chrono::duration_cast<chrono::duration<double>>(t2 - t1);
 
cout << "run time = " << time_used.count() << " seconds. " << endl;
 
return 0;
}
```

参考：[C++查看代码执行时间](https://www.cnblogs.com/yangxingsha/p/11778574.html)

**对比两个方法运行时间（开箱即用）**

```c++
#include<iostream>
#include <chrono> 
using namespace std;

int main()
{
	chrono::steady_clock::time_point t1 = chrono::steady_clock::now(); 
	//需测试运行时间的代码1

	chrono::steady_clock::time_point t2 = chrono::steady_clock::now();
	chrono::duration<double> time_used = chrono::duration_cast<chrono::duration<double>>(t2 - t1);
    cout << "run time1 = " << time_used.count() << " seconds. " << endl;
    
    t1 = chrono::steady_clock::now();
	//需测试运行时间的代码2

    t2 = chrono::steady_clock::now();
    time_used = chrono::duration_cast<chrono::duration<double>>(t2 - t1);
    cout << "run time2 = " << time_used.count() << " seconds. " << endl;
	return 0;
}
```



```c++
//计算一段程序运行的时间
#include<iostream>
#include<time.h>
using namespace std;
int main()
{
     clock_t startTime,endTime;
     startTime = clock();//计时开始
     for (int i = 0; i < 2147483640; i++)
     {
         i++;
     }
     endTime = clock();//计时结束
     cout << "The run time is: " <<(double)(endTime - startTime) / CLOCKS_PER_SEC << "s" << endl;
     system("pause");
     return 0;
}
```

参考：[c++ 查看程序运行时间](https://www.cnblogs.com/yangxingsha/p/11778574.html)



